spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydatabase
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 设置 Hibernate 在启动时自动更新数据库结构
    show-sql: true # 在控制台显示 SQL 语句

server:
  port: 8080
